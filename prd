#!/usr/bin/env bash
set -euo pipefail

# Production build shortcut
# This is equivalent to ./dev build --release

SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
exec "$SCRIPT_DIR/dev" build --release "$@"